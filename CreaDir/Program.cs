using System;
using System.IO;

namespace CreaDir
{
    class Program
    {
        static void Main(string[] args)
        {
            //Console.WriteLine("Captura el folder a crear");
            //Console.WriteLine("Captura el folder a borrar");
            Console.WriteLine("Captura el archivo a ocultar");
            string archivos=Console.ReadLine();

            /*string directorio=Console.ReadLine();
           if(Directory.Exists(directorio))
            {
                Console.WriteLine("Ya existe");
            }
            else
            {
                Directory.CreateDirectory(directorio);
            }
            Directory.Delete(directorio,true);*/
            //File.SetAttributes(archivos)
        }
    }
}